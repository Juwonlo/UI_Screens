import 'package:flutter/material.dart';

class WorkingDaily extends StatefulWidget {
  const WorkingDaily({Key? key}) : super(key: key);

  @override
  State<WorkingDaily> createState() => _WorkingDailyState();
}

class _WorkingDailyState extends State<WorkingDaily> {
  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Column(),
    );
  }
}
